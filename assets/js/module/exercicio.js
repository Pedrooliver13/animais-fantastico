export default function initExercicios() {
  // const body = document.body;
  
  // setInterval(() => {
  //   body.classList.toggle('ativo');
  // }, 2000);
  

  // const timeStart = document.querySelector('[data-time="start"]');
  // const timePause = document.querySelector('[data-time="pause"]');
  // const timeReset = document.querySelector('[data-time="reset"]');

  // const time = document.querySelector('[data-time="time"]');

  // timeStart.addEventListener('click', handleTimeStart);
  // timePause.addEventListener('click', handleTimePause);
  // timeReset.addEventListener('dblclick', handleTimeReset);
  
  // let i = 0;
  // let tempo;
  // time.innerText = 0;
  
  // function handleTimeStart() {
  //   tempo = setInterval(() => {
  //     time.innerText = i++;
  //   }, 100);

  //   timeStart.setAttribute('disabled', ''); // assim o botão fica disabilitado, e não é possível clicar;
  // }

  // function handleTimePause() {
  //   clearInterval(tempo);
  //   timeStart.removeAttribute('disabled');
  // }

  // function handleTimeReset() {
  //   i = 0;
  //   time.innerText = 0;
  // }
}

// Mude a cor da tela para azul e depois para vermelho a cada 2s.

// Crie um cronometro utilizando o setInterval. Deve ser possível
// iniciar, pausar e resetar (duplo clique no pausar).
